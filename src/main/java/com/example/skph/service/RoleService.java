package com.example.skph.service;
//@Service
//public class RoleService implements UserDetailsService {
//
//    private UserRepository userRepository;
//
//    public RoleService(UserRepository userRepository) {
//        this.userRepository = userRepository;
//    }

   // @Override
    //public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        //com.example.skph.model.User user = userRepository.findByUsername(username);

       // return org.springframework.security.core.userdetails.User.builder()
               // .username(user.getUsername())
               // .password(user.getPassword())
               // .authorities(user.getRole().stream()
                       //// .map(UserRole::getValue) // mapowanie UserRole na String
                      // .collect(Collectors.toList())
                       // .toArray(new String[0])) // przekształcenie listy ról na tablicę Stringów
               // .build();
   // }
//}
